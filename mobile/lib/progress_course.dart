import 'package:flutter/material.dart';


class OnProgressCoursesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('List of On-Progress Courses'),
      ),
    );
  }
}
